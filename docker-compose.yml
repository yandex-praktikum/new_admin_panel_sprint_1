version: "3"

services:
  postgres:
    build:
      context: .
      dockerfile:
        01_schema_design/Dockerfile
    ports:
      - "5435:5432"
    volumes:
      - $HOME/postgresql-compose-edition/data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: 123qwe
      POSTGRES_USER: app
      POSTGRES_DB: movies_database